import { WorkedPeriodDirective } from './worked-period.directive';

describe('WorkedPeriodDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkedPeriodDirective();
    expect(directive).toBeTruthy();
  });
});
