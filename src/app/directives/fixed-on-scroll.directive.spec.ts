import { FixedOnScrollDirective } from './fixed-on-scroll.directive';

describe('FixedOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
