import { ElapsedPeriodDirective } from './elapsed-period.directive';

describe('ElapsedPeriodDirective', () => {
  it('should create an instance', () => {
    const directive = new ElapsedPeriodDirective();
    expect(directive).toBeTruthy();
  });
});
